import {create} from "zustand"

const useWishlistStore = create((set, get)=>({
    wishlist: [],
    addToWishlist: (set) => {
            
        },
    removeFromWishlist: (set) => {
                
        },
    clearWishlist: (set) => {
        
    },
}))